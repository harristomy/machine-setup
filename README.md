# Ubuntu install commands

Currently these scripts are hardcoded for x86_64, adjust as necessary

```bash
wget https://raw.githubusercontent.com/harristomy/machine-setup/main/default-ubuntu-setup.sh && chmod +x default-ubuntu-setup.sh && bash default-ubuntu-setup.sh && rm default-ubuntu-setup.sh &&  wget https://raw.githubusercontent.com/harristomy/machine-setup/main/neovim-setup.sh &&  chmod +x neovim-setup.sh &&  bash neovim-setup.sh &&  rm neovim-setup.sh
```
